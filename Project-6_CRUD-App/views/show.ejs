<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.3.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/CSS/style.css">
    
    <title>Home page</title>
</head>
<body style="padding: 5% 10% 0% 10%;">
    <div class="text-center">
            <div class=" mx-auto d-flex flex-row justify-content-start">
                <a href="/users" type="button" class="btn btn-secondary btn-small align-self-center">&larr;</a>
                <div  class="px-3"><h3>View Employee</h3></div>
        </div>
        <div  class="p-1">

            <table class="table table-bordered text-start">
                <thead class="thead-dark">
                  <tr>
                    <th scope="col">Emp. Id</th>
                    <th scope="col">Name</th>
                    <th scope="col">Email</th>
                    <th scope="col">Phone</th>
                    <!-- <th scope="col">Comment</th> -->
                    <th scope="col" class="text-end">Action</th>
                  </tr>
                </thead>
                <tbody>
                        <tr>
                            <td><%= foundRow.id %></td>
                            <td><%= foundRow.name %></td>
                            <td><%= foundRow.email %></td>
                            <td><%= foundRow.phone %></td>
                            
                            <td class="text-end">
                                <a href="/users/<%= foundRow.id %>/edit" type="button" class="btn btn-light btn-small"><i class="bi bi-pencil"></i>Edit</a>
                                <a href="/users/<%= foundRow.id %>/delete" type="button" class="btn btn-light btn-small"><i class="bi bi-person-x"></i> Delete</a>
                            </td>
                        </tr>
                </tbody>
            </table>
            <p class="text-start">
            <b>Comments: </b><%= foundRow.comment %>
            </p>
        </div>
</div>
</body>
</html>